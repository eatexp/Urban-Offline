## 2026-05-02 - Added ARIA labels and titles to AIChat header buttons
**Learning:** It is crucial to ensure that all icon-only buttons have both an `aria-label` for screen readers and a `title` for sighted users (as tooltips).
**Action:** When adding or modifying icon-only buttons, consistently include these attributes to improve accessibility and usability.
